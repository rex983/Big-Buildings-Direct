# Database (Supabase PostgreSQL)
# Pooled connection for runtime (pgbouncer transaction mode, port 6543)
DATABASE_URL="postgresql://bbd_prisma.PROJECT_REF:PASSWORD@aws-1-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
# Direct connection for migrations (session mode, port 5432)
DIRECT_URL="postgresql://bbd_prisma.PROJECT_REF:PASSWORD@aws-1-us-east-1.pooler.supabase.com:5432/postgres"

# Authentication
AUTH_SECRET="your-auth-secret-here-generate-with-openssl-rand-base64-32"
AUTH_URL="http://localhost:3000"

# File Storage
STORAGE_ADAPTER="local" # local | s3 | supabase
UPLOAD_DIR="./uploads"

# AWS S3 (if using S3 adapter)
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_REGION="us-east-1"
AWS_S3_BUCKET=""

# Supabase (if using supabase adapter)
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
SUPABASE_SERVICE_ROLE_KEY=""

# Email (Resend)
RESEND_API_KEY=""
EMAIL_FROM="noreply@bigbuildingsdirect.com"

# App URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Socket.IO
SOCKET_PORT=3001

# Google Gemini AI
GEMINI_API_KEY=""

# Order Processing Webhook (shared secret with Python order processor)
ORDER_WEBHOOK_SECRET=""
